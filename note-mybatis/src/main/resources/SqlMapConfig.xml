<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
    <!--  配置环境  -->
    <environments default="mysql">
        <!--  配置环境  -->
        <environment id="mysql">
            <!--  配置事务类型  -->
            <transactionManager type="JDBC"/>
            <!-- 配置数据源，连接池  -->
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.cj.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/test_schema"/>
                <property name="username" value="root"/>
                <property name="password" value="root"/>
            </dataSource>
        </environment>
    </environments>

    <!--  指定注解源配置文件  -->
    <mappers>
        <!-- 使用配置文件方式 -->
        <mapper resource="com/randy/note/example/dao/UserDao.xml"/>

        <!-- 使用注解方式 -->
        <mapper class="com.randy.note.example.dao.UserDaoAnno"/>

        <mapper resource="com/randy/note/start/UserMapper.xml"/>

        <mapper resource="com/randy/note/start/UserMapperKey.xml"/>

        <mapper resource="com/randy/note/resultmap/BookMapper.xml"/>
    </mappers>
</configuration>